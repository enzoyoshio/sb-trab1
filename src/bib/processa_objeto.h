// fazer o processamento para objeto aqui
#pragma once
#include <string>

namespace processa_objeto {
    void print();
    
    void processa(std::string filename);
    // tem que arrumar a assinatura depois
    void passagem1(std::string filename);
    void passagem2(std::string filename);
}
